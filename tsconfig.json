{
	"extends": "./.svelte-kit/tsconfig.json",
	"compilerOptions": {
		"checkJs": false, // Don't want to type check JS files
		"resolveJsonModule": true
	},
	"include": ["src/**/*.d.ts", "src/**/*.ts", "src/**/*.svelte"]
}
